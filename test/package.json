{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/inject.js --bundle --format=iife --outfile=dist/inject.bundle.js",
    "watch": "npm run build -- --watch",
    "test": "npm run test:unit && npm run test:tokenizer && npm run test:leak",
    "test:unit": "node src/smoke.js",
    "test:tokenizer": "node src/tokenizer-test.js",
    "test:leak": "npm run build && node src/leak-test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
