{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/inject.js --bundle --outfile=dist/inject.bundle.js --platform=browser && esbuild src/content-bridge.js --bundle --outfile=dist/content-bridge.bundle.js --platform=browser",
    "test": "node src/smoke.js && node src/adapter-smoke.js && node src/message-validator.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
