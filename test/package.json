{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/inject.js --bundle --format=iife --outfile=dist/inject.bundle.js",
    "watch": "npm run build -- --watch",
    "test": "npm run test:unit && npm run test:tokenizer && npm run test:endpoint && npm run test:leak && npm run test:vault && npm run eval:check",
    "test:unit": "node src/smoke.js",
    "test:tokenizer": "node src/tokenizer-test.js",
    "test:leak": "npm run build && node src/leak-test.js",
    "test:endpoint": "npm run build && node src/endpoint-test.js",
    "test:vault": "npm run build && node src/vault-test.js",
    "eval": "node eval/validate.js && node eval/score.js",
    "eval:validate": "node eval/validate.js",
    "eval:bench": "node eval/bench.js",
    "eval:save": "node eval/validate.js && node eval/score.js --save",
    "eval:check": "node eval/validate.js && node eval/score.js --check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
